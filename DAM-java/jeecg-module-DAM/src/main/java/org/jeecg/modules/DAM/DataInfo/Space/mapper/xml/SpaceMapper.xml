<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.DAM.DataInfo.Space.mapper.SpaceMapper">

    <!-- 更新空间成员数量 -->
    <update id="updateUserCount">
        UPDATE dam_space SET user_count = #{userCount} WHERE id = #{spaceId}
    </update>

</mapper>